## Secret download serves the newest release asset
status: done
branch: fix/secret-download
pr: https://github.com/Jack-Attackz101/eyerest/pull/16
changed: website/api/build.js, website/index.html, .conductor-status.md
skipped: Nothing asked for. Two findings that need a human rather than code. (1) This repo has no GitHub releases at all, /releases/latest is a 404, so after this merges the panel keeps serving the committed DMG and now says "no release published yet, serving the committed DMG" under it. The fix is correct but changes nothing visible until a release exists with a .dmg asset attached. (2) ADMIN_PASS is not set in the Vercel project, so the fallback value in the source is what is live, and that value is committed to a public repo. The panel and the download are therefore open to anyone who reads the code. Nothing in this change made that worse and nothing in it can fix it; the env var needs setting in Vercel.
blocked_on: none
verified: Walked the flow on production in a real browser with downloads captured to disk BEFORE changing anything. Steps 1 to 4 all work; step 5 is the break and it is not a 404, it downloads a genuine mountable disk image (16,876,949 bytes, UDIF, Iris.app 1.0.0) that is simply the 29 July binary, 12 days behind the sources. Production also answered 401 with no password and 401 with a wrong password, and the page source for a logged-out visitor contained no download URL. After the change: the real handler was run against a stubbed GitHub API for every branch, 21 assertions passing, covering 401 for no, empty and wrong passwords with no download details in the 401 body, a release with a dmg returning the asset URL, tag label, size and published date and picking the dmg over the first asset, a release with no dmg falling back with that reason, no release falling back with that reason, a thrown fetch and a 403 both falling back with honest notes rather than erroring, the five minute cache making no second GitHub call, a failure not being cached, the 7 day threshold flagging 12 days and not flagging 1, and POST being refused. The release parsing was then run against a real GitHub /releases/latest payload rather than my own fixture, and a real release asset URL was fetched through its redirect to confirm it returns application/octet-stream with dmg magic bytes rather than an HTML error page, hdiutil reporting UDZO. The full click path was walked in a browser for both paths with downloads captured: the fallback panel reads "committed DMG, built Jul 29 2026, 16.1 MB, served ..." with the note and the 12 days warning and yields a complete file with a koly trailer, and with GitHub answering with a real release the panel reads "GitHub release v3.0.11, built Aug 9 2026, 7.1 MB" with no warning and the click downloads the asset as an attachment. The hero waitlist suite still passes, 27 assertions. Could not verify: the release path against this repo's own releases, because there are none, so another public repo's real release was used to exercise the same code; and the Vercel preview for this PR, because preview deployments are behind Vercel deployment protection and redirect to vercel.com/sso-api, so they cannot be reached anonymously. Production is public, which is why the before walk was possible at all.
finished: 2026-08-11T23:23:30Z

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and website/api/build.js both exist. fix/secret-download was cut fresh from origin/main, which by then had PRs #12, #13 and #15 merged.
- To make the download actually current, publish a release with Iris.dmg attached. The endpoint picks the first asset whose name ends in .dmg from /releases/latest, so the file name does not matter.
- The bento feature layout, PR #14, is still open and unmerged. It edits the features section of website/index.html; this branch edits the admin panel script and one CSS line, so they do not overlap.
- The dmg-freshness CI check will stay red until the committed DMG is rebuilt or removed. It compares the committed binary against the sources and knows nothing about releases, so once releases are the real download that check is measuring something the site no longer serves. Worth revisiting then.

---

## Hero waitlist box
status: done
branch: hero/waitlist-box
pr: https://github.com/Jack-Attackz101/eyerest/pull/15
changed: website/index.html, .conductor-status.md
skipped: Nothing asked for. Two notes on how the scope rule was kept: the form at the bottom was not touched at all, so the shared handler finds both forms by id rather than by adding an attribute to the lower one, and the hero form declares its own success behaviour with data-collapse-on-success. The one line added inside the existing mobile media query is hero-scoped (.hero-waitlist .wl-submit), which was unavoidable to make the button full width when the row stacks.
blocked_on: none
verified: Headless Chrome over the DevTools protocol against a local stand-in for the Vercel runtime, with the waitlist endpoint stubbed to exercise the new, duplicate and refused branches. 27 assertions, all passing. Structure: hero children in the required order (wordmark, slogan, supporting line, box, note, link); the supporting line is two clauses and says it is not out; placeholder you@example.com and button "Join the waitlist"; input and button on one row at equal height on desktop; the box reuses .waitlist-input and .wl-submit; the reassurance line is one short sentence at 11px in a muted colour; "See how it works" has no background and no shadow and carries an arrow, the submit button is the only solid element in the hero, and the link is centred under the box; nothing is autofocused; nothing overlaps and the whole hero fits the viewport. Behaviour: exactly one fetch to /api/waitlist in the file; the hero refuses an invalid address before posting; success collapses the box and leaves the confirmation in place without scrolling or opening a dialog; both forms work independently in both orders; both report a duplicate identically and a server refusal identically and stay usable after the error. Content: no price, no em dash, no countdown, no invented signup count, nothing sticky or fixed. At 390px the row stacks, the button goes full width, nothing overflows and it still submits. Scope was checked mechanically rather than by eye: nav through hero, header close through the script tag, features through the admin panel, and the CSS from the reveal block to the end of the stylesheet are all byte-identical to main apart from the one hero-scoped mobile rule. Screenshots at 1280px and 390px plus the post-signup state were reviewed. Not verified: the live deployment, since the Vercel preview is behind deployment protection and needs a logged-in browser.
finished: 2026-08-11T23:09:50Z

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and website/api/build.js both exist. hero/waitlist-box was cut fresh from origin/main.
- Two waitlist forms now share one wire(form) function. If a third is ever added, give it a .waitlist-row, a .waitlist-msg, an input[type=email] and a submit button, then add its id to the list at the bottom of that function.
- The bento box feature layout is a separate branch and PR that is not merged: bento-box-sections-layout, https://github.com/Jack-Attackz101/eyerest/pull/14. It also edits website/index.html, in the features section, so it and this branch will both want main. They touch different parts of the file.
- CI: the placeholder check passes. The dmg-freshness check fails, as it does on main and every branch, because the shipped DMG is behind the source.

---

## Website rewrite - waitlist page
status: done
branch: redesign/waitlist-page
pr: https://github.com/Jack-Attackz101/eyerest/pull/13
changed: website/index.html, website/api/build.js (new), website/js/iris-demo.js (deleted), .conductor-status.md
skipped: Nothing from the six tasks. Two deliberate calls that are not what a literal reading would give: (1) the dark stats band still sits between features and how it works, where it already was. The required section order does not mention it and removing it would have changed the section rhythm the brief said to keep. All named sections are in the required order. (2) The build date in the admin panel is shown twice, labelled. The brief says to read it from the file's Last-Modified header, but Vercel stamps that header at deploy time, not build time, so a warning driven off it alone would never fire. The panel shows "served" from the header as asked and "built" from the commit that last touched website/Iris.dmg, and the staleness warning compares the build date against the newest commit under Iris/, the same comparison the DMG freshness CI check makes. website/images/cloudbkng.png is now unreferenced since it was only used by the deleted demo; left in place because deleting assets was not asked for.
blocked_on: none
verified: Served website/ locally through a stand-in for the Vercel runtime that runs the real api/build.js, then drove headless Chrome over the DevTools protocol. 34 assertions, all passing: page loads with no console errors (the only network error logged is the 401 the harness asks for on purpose with a wrong admin password); section order is hero, features, how it works, see it in action, waitlist, questions, footer; zero prices, zero Iris.dmg anywhere in the DOM, zero em dashes; hero has exactly one interactive element and it is centred under the slogan; nav CTA is "Join waitlist" pointing at #waitlist; the Supademo comment is where the demo was and the demo markup, script and importmap are gone; 26 feature items across 5 groups, each with a name and a sentence, both train photos kept, the unheaded paragraph gone; six details/summary questions with the default triangle removed, all starting closed; under real key events the summary takes focus, Enter opens, Space opens a second one while the first stays open, Enter closes; a real mouse click 30 px inside the right edge, past the question text, toggles the row; with prefers-reduced-motion emulated the accordion still opens and closes; the gated download renders no download element at all for a wrong password and shows the list, /Iris.dmg with a download attribute, "built Jul 29, 2026" and "This build is 11 days behind the source" for the right one; the waitlist form posts and confirms. Screenshots of hero, features, questions, waitlist and the unlocked admin panel were reviewed against the old design. Two harness bugs were found and fixed during this (a miscount and a smooth-scroll race), not page bugs. The harness lives in this workspace's gitignored .context/site-harness and is not committed. Not verified: the live deployment. The Vercel preview built fine but every request to it redirects to vercel.com/sso-api, so it is behind Vercel deployment protection and cannot be fetched without being logged in. The preview is where the served header and the GitHub API lookup first run against the real thing, so open it in a logged-in browser before merge: click the footer logo, enter the admin password, and check that the build row reads "built 29 Jul 2026 · served <the last deploy>" with the 11 days behind warning under it.
finished: 2026-08-10T22:22:47Z

Notes for the next session:

- Branch check passed before any work: Iris/FlowDetector.swift exists and SettingsComponents.swift contains SettingsTabBar. redesign/waitlist-page was cut fresh from origin/main.
- website/api/waitlist.js was not touched. The new endpoint reads the same ADMIN_PASS env var, so there is no second secret and no password in client-side JavaScript.
- Prompt 1 of the app build plan, the nudge budget, is a separate branch and PR: feat/nudge-budget, https://github.com/Jack-Attackz101/eyerest/pull/12. It is not merged, so it is not in this branch.
- CI on the PR: the placeholder check passes. The dmg-freshness check fails, as it does on main and every other branch, because the DMG shipped on the website predates the source by 11 days. That staleness is now visible in the admin panel, which was the point of task 6.
- There is a stash on this workspace from an earlier session: "wip on fix-dmg-bg-scene-only before feat/nudge-budget". It contains uncommitted edits to website/index.html among other files, so it will conflict with this rewrite if it is ever popped. Worth reviewing rather than applying blind.

---

## Prompt 1 - Nudge budget
status: done
branch: feat/nudge-budget
pr: https://github.com/Jack-Attackz101/eyerest/pull/12
changed: Iris/Engines/NudgeBudget.swift (new), Iris/AppDelegate.swift, Iris/Controllers/WindDownController.swift, Iris/Controllers/DeskResetController.swift, Iris/Views/Settings/DemoSettingsSection.swift, .conductor-status.md (new)
skipped: NudgeEngine, the periodic posture nudges, is not routed through the budget. It was not one of the five named callers, it does not call showTransientNudge, and it owns a 60 second pill with its own show/hide/reschedule loop, so routing it needs a denial callback and a rework of its visibility bookkeeping rather than a gate in front of a detector. It remains a nudge source that can stack with budgeted ones. The DEBUG demo rows also still call showTransientNudge directly, on purpose, since their job is to bypass the gate.
blocked_on: none
verified: Debug and Release both build clean with no warnings (xcodebuild, macOS). Behaviour checked by compiling the real Iris/Engines/NudgeBudget.swift unmodified against stub TimerEngine, FlowDetector and DebugConfig collaborators, fast-cycle on so 8 minutes is 8 seconds, and driving 20 assertions: every rule 3 and rule 4 block (idle, resting, suspended, overlay on screen, last minute before a break, in flow), three requests coalescing into one nudge, priority order in the combined text, the lowest priority request dropped as crowdedOut, the minimum gap refusing then clearing, a pending nudge dropped when flow starts mid-window without spending hourly budget, the fourth nudge allowed and the fifth refused as hourlyCap, and every possible combined line fitting the 44 character menu bar limit. All 20 pass. That last assertion caught a real defect, a 47 character pair, which is fixed. The harness lives in this workspace's gitignored .context/nudge-budget-harness and is not committed. Not verified in the running app: the budget gates on multi-hour timers, so watching it accept and refuse for real needs a long session with the DEBUG counters open.
finished: 2026-08-10T22:04:34Z

Notes for the next session:

- Branch check against origin/main passed before any work: Iris/FlowDetector.swift exists, SettingsComponents.swift contains SettingsTabBar, LicenseWindowController.swift contains $14 and not $35. The workspace was sitting on fix-dmg-bg-scene-only, 16 commits behind main, so feat/nudge-budget was cut fresh from origin/main. Uncommitted license, pricing, DMG and website edits that were in the working tree were stashed, not discarded (git stash list, message "wip on fix-dmg-bg-scene-only before feat/nudge-budget").
- Judgement calls that reviewers should look at are written up in the PR: the 90 second coalescing hold, the menu bar text limit moving from 22 to 44 characters, combined text ordering by priority, and "look away - 20 seconds" losing its em dash to satisfy the brand rule.
- CI on the PR: the placeholder check passes. The dmg-freshness check fails, but it fails on main and on every other branch too, because the DMG shipped on the website predates the source. Pre-existing and untouched by this work.
- Prompts 2 through 9 were deliberately not started.
