## Pricing, stretch cards with the countdown, and the download
status: done
branch: feat/pricing-section, feat/stretch-with-countdown, fix/download-must-be-latest
pr: https://github.com/Jack-Attackz101/eyerest/pull/24, https://github.com/Jack-Attackz101/eyerest/pull/25, https://github.com/Jack-Attackz101/eyerest/pull/26
changed: website/index.html, website/api/build.js, Iris/Views/BlackoutView.swift, Iris/Controllers/BlackoutController.swift, Iris/Models/BreakTheme.swift, Iris/Models/StretchCard.swift, docs/pricing/*.png (new), docs/break-screen/*.png (new), docs/secret-download/*.png (new), .conductor-status.md
skipped: Nothing from the three jobs. Out of scope by the brief and not attempted: Stripe checkout wiring, licence validation in the app, hiding the break from screen sharing, and the Settings redesign. On the question the brief asked to be challenged, selling a paid tier without licence validation: I agree with the brief and said so in PR 24. LicenseManager still speaks Gumroad, there is no Stripe integration, and the app has no way to know whether someone paid, so a buy button would either take money for something that cannot unlock or unlock for everyone. The honest launch is the free app live, the prices visible, and the buttons pointing at the waitlist until licensing is tested, which is exactly what shipped.
blocked_on: none
verified: Job 1, 23 browser assertions: section order with pricing between see it in action and waitlist; three cards in the order Free, 12, 3; the middle one alone is featured, taller, green bordered, with the larger number and the only solid button; the flag avoids the word recommended; all three CTAs resolve from one config object; the free card carries no trap words; free lists six features and both paid tiers list seven leading with the blocker; each price appears exactly once; no fake urgency or invented counts; zero em dashes; the new question answers no; the hero and the other six questions untouched; at 390px they stack and share a width with no overflow. Job 2, 30 assertions plus rendered screenshots on all five themes: the countdown keeps its position because the card is anchored below rather than added to its stack; the card clears 4.5:1 for title and instruction on every theme and is distinguishable from the background; box breathing suppresses the card and a rest too short for a lap leaves the card in place; zero em dashes in StretchCard.swift. Job 3, 28 endpoint assertions plus live checks the brief insisted on: no password 401, wrong 401, right 200; the served bytes are a real disk image, octet-stream with 78da magic bytes, mounting as UDZO, not an HTML error page; the reported SHA256 matched the downloaded file exactly on both paths; the release path was exercised against exelban/stats v3.0.14, a real public release payload rather than a fixture, and the hash survived the redirect to release-assets.githubusercontent.com; the whole path walked in a browser both ways with the download captured. A correction: the theme harness's luminance function weighted gamma-encoded channels with no sRGB linearisation, so every contrast ratio it printed before this session was wrong, including numbers quoted in PR 20 and in the entry below. Recomputed properly, white on the Dusk top colour is 4.66:1 and passes, not the 2.4:1 I reported, while Tide is 3.11:1 and genuinely failed. The gradient change in PR 20 was therefore necessary for Tide and merely harmless for Dusk, and the outcome, content at 15.5:1 on both, is unaffected. Could not verify: two displays during a break, since one panel was rendered rather than two, though both share a single model by construction; a live break in the running app; and the release path against this repo's own releases, because there still are none.
finished: 2026-09-01T20:36:35Z

Notes for the next session:

- Branch check passed: BlinkEngine.swift, BoxBreathingView.swift and BreakTheme.swift all exist. All three branches were cut from origin/main.
- The brief says three days to launch on 30 August, and today is 1 September, so that date has passed. Nothing in these three jobs depends on it, but the paid tiers still cannot be switched on until licensing exists.
- Publishing a release with a .dmg attached now needs no code change. The panel switches to it, shows the tag, date, size and hash, and the red fallback line disappears. If the .dmg is missing from the release it says so loudly, which it used to hide.
- The theme harness in .context/breath-harness now computes WCAG luminance correctly. Any contrast number quoted from before this session should be recomputed before it is trusted.
- Still open: licence validation, Stripe wiring, hiding the break from screen sharing, and confirming a break really is absent from a QuickTime recording, which no harness can check.

---

## Settings panel rebuild
status: done
branch: redesign/settings-panel
pr: https://github.com/Jack-Attackz101/eyerest/pull/21
changed: Iris/Views/SettingsView.swift, Iris/Views/Settings/SettingsComponents.swift, Iris/Views/Settings/SettingsSummary.swift (new), Iris/Views/Settings/SettingsChoiceRow.swift (new), Iris/Views/Settings/HabitsSettingsSection.swift, Iris/Views/Settings/FocusSettingsSection.swift, Iris/Views/Settings/ChallengeSettingsSection.swift, Iris/Views/Settings/LicenseSettingsSection.swift, Iris/Views/Settings/DemoSettingsSection.swift, docs/settings-redesign/*.png (new), .conductor-status.md
skipped: Nothing from prompt two. Three notes. (1) The fifth destination is a tab rather than a sheet, which the brief prefers, and it is labelled "Rituals" rather than "Routines": five tabs leave about 61pt each at the 320pt popover width, and at 9pt "Routines" does not fit and would have truncated, which the screenshots would have shown. (2) The summary shows the cycle plus at most two of the things that change when a break lands, not every enabled feature. With six on it stopped being a sentence and became a five line paragraph, which is the thing it was meant to replace. (3) Onboarding still uses SettingStepperRow for break interval and rest duration, in Iris/Views/Onboarding/OnboardingSteps.swift. Prompt two scoped the work to the Settings panel, so it was left alone, but it means a new user meets the old stepper once during setup and never again. Worth a follow-up.
blocked_on: none
verified: Debug and Release both build clean with no warnings, and all five tabs were rendered from the real SettingsView at exactly 320x380 and reviewed as images, which the brief asks for because nobody had seen the panel running. The screenshots are committed under docs/settings-redesign and embedded in the PR. The harness compiles all 60 real sources and stubs only the four collaborators that reach outside the panel: UpdateChecker needs Sparkle, LicenseManager talks to Gumroad, PostureCameraEngine would ask for the camera, and AppDelegate is the app. Looking at the output rather than trusting it caught two real rendering bugs that assertions would not have: captions wrapping to two lines were clipped by SettingRow's fixed 44pt height and truncated with an ellipsis, so "Hide breaks from screen sharing" rendered as "Hide breaks from screen...", now fixed with minHeight and fixedSize; and every switch drew in its inactive grey because the harness window could not become key, which would have misrepresented the panel in the review. Constraints checked mechanically rather than by eye: all six component property lists in SettingsComponents.swift are unchanged, with SettingToggleRow gaining one optional parameter after the existing ones, so all eleven toggle rows in HabitsSettingsSection and the other five section files compile untouched; the set of engine bindings the settings UI references is identical to main apart from one addition, popoverStatus, which the summary reads, so nothing was culled; zero em dashes remain anywhere in the settings UI, including one that was user-facing in the licence row; the palette is unchanged and the fifth tab reuses the existing irisTintSchedule. Post-merge check of main after PRs #19, #20 and #21 landed independently over overlapping files: both configurations build, all three feature sets coexist, and all six harness suites pass. Could not verify: the panel opened from the menu bar popover in the running app rather than from the harness, and interaction, since the pills, the Custom slider and tab switching are captured as rendered states rather than exercised by clicks.
finished: 2026-08-25T01:33:06Z

Notes for the next session:

- Both PDFs are now fully landed. PR #19 the notch, #20 items 2 to 12 of prompt one, #21 the Settings rebuild. Main builds clean and every harness suite passes against it.
- Do Not Disturb, item 7 of prompt one, is the one thing deliberately not built, with the evidence in the #20 entry below. If it is wanted it should be SetFocusFilterIntent and an App Intents extension, not a read of the private Assertions store.
- The settings harness lives in the gitignored .context/settings-harness. It renders any tab at the real popover size and screenshots the window only, so no desktop ends up in a public PR. Reuse it for any further panel work, and re-shoot docs/settings-redesign when the panel changes.
- Still open before launch on 30 August: the DMG is a hand-committed binary and the freshness check is red on every branch, and there are no GitHub releases, so the gated download serves the committed DMG and says so. Publishing a release with a .dmg attached is what switches that over, with no code change.
- Worth doing on Jack's machine before recording the demo: trigger a break during a QuickTime screen recording to confirm the overlay really is absent, since that is the one thing about item 8 that a harness cannot check.

---

## Prompt one, items 2 to 12
status: done
branch: feat/prompt-one-rest
pr: https://github.com/Jack-Attackz101/eyerest/pull/20
changed: Iris/Models/BreakTheme.swift (new), Iris/Views/BoxBreathingView.swift (new), Iris/Engines/BlinkEngine.swift (new), Iris/Controllers/BlinkCueController.swift (new), Iris/Support/GlobalHotkey.swift (new), Iris/Views/Settings/HotkeyRecorderRow.swift (new), website/api/_ratelimit.js (new), Iris/Views/BlackoutView.swift, Iris/Controllers/BlackoutController.swift, Iris/Controllers/BlockerController.swift, Iris/Controllers/WarningPillController.swift, Iris/Views/WarningPillView.swift, Iris/TimerEngine.swift, Iris/StatsEngine.swift, Iris/NudgeEngine.swift, Iris/Engines/NudgeBudget.swift, Iris/AppDelegate.swift, Iris/Views/SettingsView.swift, Iris/Views/Settings/HabitsSettingsSection.swift, website/api/build.js, website/api/waitlist.js, .conductor-status.md
skipped: Item 7, Do Not Disturb, deliberately, which is what the brief asks for when Focus cannot be read reliably. Evidence rather than assumption: com.apple.notificationcenterui doNotDisturb is the pre-Monterey mechanism and does not track modern Focus, and the only live source on macOS 26.5 is ~/Library/DoNotDisturb/DB/Assertions.json, which is readable but contains storeInvalidationRequestRecords and storeInvalidationRecords, a log of Focus being turned off rather than a flag for whether it is on. There is no public read API. Parsing an undocumented store whose shape has already changed across releases means Iris interrupts a presentation the first time Apple changes it, which is the exact failure the brief calls unacceptable. The correct route is SetFocusFilterIntent, which is public and reliable but needs an App Intents extension and is its own piece of work. Prompt two, the Settings rebuild, was also not started: the brief says to send it as its own session after prompt one lands.
blocked_on: none
verified: Debug and Release both build clean with no warnings, and 100+ assertions across five harnesses all pass. The pill was verified on real non-notch hardware this time, which PR #19 could not do: an AirPlay display is connected and natively reports safeAreaInsets.top 0.0, so the fallback path ran on a display that genuinely has no notch, and the pill starts 0.0pt from the physical top, 324pt wide on its first row (240 plus the 84pt snooze control) with square top corners. The notch display is still flush at 373pt. Box breathing and themes, 30 assertions: a lap is 16 seconds and a side 4, t=4/8/12 land exactly on the corners, t=2 is exactly halfway along the bottom so the rate is linear, the word changes at 4.0 and not 3.99, 16 seconds fits and 15 does not, and every theme clears 4.5:1 for both the countdown and the breathing square. Dusk and Tide were exactly as risky as the brief warned, at 2.4:1 and 1.7:1 against the named brand colours, so the gradient now reaches its deep end above the content and both sit at 5.8:1. Idle and snooze, 14 assertions: every value from 1 to 299 seconds does nothing, 300 holds the clock, an hour credits a natural break, the third snooze is refused, and the allowance returns after a break. Hotkey, 10 assertions against the real Carbon API: Option Command B registers, a duplicate reports eventHotKeyExistsErr rather than being accepted silently, no-modifier is refused with the right message, and unregistering frees the combination. Rate limiting, 27 assertions against the real handlers with an in-memory KV, plus an end-to-end browser walk: five 401s then 429 with Retry-After 900, the right password refused while locked out, another IP unaffected, a success clearing the count, the public count and signup unaffected, and the attempted password never stored. Item 12 was a verification pass on work already merged in PR #16. Could not verify: item 8 against a real QuickTime recording, which needs a screen recording of a triggered break on Jack's machine and is worth doing before the demo; the blink cue and box breathing running in a live break rather than in their harnesses; and the hotkey actually firing, which needs a human at the keyboard.
finished: 2026-08-24T20:10:26Z

Notes for the next session:

- Branch check passed before any work: NudgeBudget.swift and FlowDetector.swift exist, and index.html contains hero-waitlist and bento-cell. feat/prompt-one-rest was cut while PR #19 was still open, so it stacked on it; #19 has since merged and the branch is now exactly five commits ahead of main with no divergence.
- Prompt two, the Settings rebuild, is the remaining work and is its own session. It touches the same files as several items here, so merge this first.
- The pill harness in .context/pill-harness now captures every display and prints which one the pill landed on. With an external display connected it is a genuine non-notch test rig, which is how the last open question from PR #19 got closed.
- NudgeBudget now has request(_:text:present:onRefused:). Any future periodic nudge source should use it and reschedule on refusal rather than dropping, or refusing one nudge ends the whole series.
- Do Not Disturb, item 7, is the one thing in prompt one that is not built. If it is wanted, it should be SetFocusFilterIntent and an App Intents extension, not a read of the private Assertions store.

---

## Notch pill flush on every Mac
status: done
branch: fix/notch-pill-flush
pr: https://github.com/Jack-Attackz101/eyerest/pull/19
changed: Iris/Views/WarningPillView.swift, Iris/Controllers/WarningPillController.swift, .conductor-status.md
skipped: Items 2 to 12 of prompt one, deliberately. The brief says group 1 is the notch because the pill is in every frame of the demo Jack is about to record, so this session is that group only. Next is box breathing. Two judgement calls inside the job: the gap constant M.fallGap was deleted rather than set to 0, because a knob that must always be zero is a knob someone later sets back to 8, so flushness is now structural rather than a value; and the controller and the view were made to size from one PillMetrics enum rather than two sets of literals, which had already drifted (the controller said 110 where the view said sideExt * 2).
blocked_on: none
verified: The brief's diagnosis was right and incomplete. The fallback was indeed rendering, a RoundedRectangle offset down 8pt with a shadow, because safeAreaInsets.top is 0 on that machine. But fixing the shape and the gap still left the pill 33pt low, and the cause is that AppKit silently constrains every window frame so it cannot cover the menu bar: the panel asked for y 912 on a 956pt screen and its frame came back 879, exactly the 33pt the menu bar occupies. positionPanel was already using frame and never visibleFrame as the brief requires, and visibleFrame was being applied to it anyway. PillPanel overrides constrainFrameRect to return the rect unchanged. That second cause would have made the notch case wrong too, and nobody had looked at it. Proof is pixels, not opinion: a harness compiles the real WarningPillController and WarningPillView unmodified against a stub TimerEngine and swizzles NSScreen.safeAreaInsets to zero, so the real controller takes the broken branch on hardware that does have a notch, then screenshots the top 200pt and measures the image. Before, simulated no-notch: pill starts 41.0pt from the physical top, 192 of 240pt wide on its first row, top corners rounded. After, simulated no-notch: 0.0pt, flush, 240 of 240pt, square. After, this Mac's real notch: 0.0pt, flush, 289 of 289pt, square. The startup log prints the screen, its frame, safeAreaInsets.top, the resolved hasNotch and the pill size, on both paths. Debug and Release both build clean with no warnings. Could not verify: real non-notch hardware, since the swizzle reproduces the detection branch and the rendering but not a display that genuinely lacks a notch, so one look on Jack's machine before recording is worth it; and the pill inside the running app rather than the harness. The screenshots are not attached to the PR because they captured the desktop behind the pill, including a chat window.
finished: $STAMP

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and Iris/FlowDetector.swift exist, and website/index.html contains hero-waitlist and bento-cell. fix/notch-pill-flush was cut fresh from origin/main.
- The constrainFrameRect trap applies to any window meant to cover the menu bar. If the blackout, the blocker overlay or a future blink overlay ever look a menu bar too low, that is the first thing to check.
- The pill harness lives in the gitignored .context/pill-harness and is not committed. It compiles the real pill sources against a stub TimerEngine, takes SIMULATE_NO_NOTCH=1, and writes a measured screenshot. Reuse it for anything that has to sit on the screen edge.
- Remaining in prompt one, in order: 2 box breathing (exact square-perimeter shape specified), 3 blink engine, 4 idle detection, 5 snooze on the warning pill, 6 global hotkey, 7 Do Not Disturb, 8 hide from screen sharing, 9 break themes, 10 route NudgeEngine through the budget, 11 rate limit the admin panel, 12 confirm the secret download picks up a release. Prompt two is the Settings rebuild and is its own session.
- Item 12 is already implemented and merged as PR #16, so it should mostly be a verification pass once Jack publishes a release with a DMG attached.

---

## Job 1 - Fail closed when ADMIN_PASS is missing
status: done
branch: fix/fail-closed-admin-pass
pr: https://github.com/Jack-Attackz101/eyerest/pull/18
changed: website/api/build.js, website/api/waitlist.js, .conductor-status.md
skipped: Nothing asked for. Three judgement calls. (1) The 500 guard sits in front of the password compare on the gated paths only, so the public count and signing up still work when ADMIN_PASS is missing. Taking the signup form down because an admin variable is unset would turn a config mistake into lost waitlist entries, and neither of those paths touches the password. (2) PR #17, "Security: fail closed when ADMIN_PASS is missing", is open and covers the same ground, but it fails closed to 401 rather than 500. A 401 is indistinguishable from a wrong password, so a broken gate looks like a typo and nobody investigates. This branch was cut from main as the job says, so one of the two should be closed rather than both merged; they touch the same two lines. (3) Three em dashes in waitlist.js were rewritten while in the file, one of them in the user-facing 503 message, since the project rule is no em dashes anywhere.
blocked_on: none
verified: Ran both real handlers twice, once with ADMIN_PASS set and once with it deleted, 21 assertions all passing. With it set: the correct password still returns 200 and the build JSON, a wrong password 401s, no password 401s, and the old published literal 401s, on both endpoints; the public count still needs no password. With it deleted: the old literal gets 500 and a generic "Server misconfigured" on both endpoints, any password gets 500, no password gets 500, DELETE gets 500, the 500 body mentions nothing about the config or the download, the missing secret is logged once per file at load, and no log line ever contains a password value. Source-level: neither file contains the old literal, neither reads the variable with a fallback, each reads it exactly once, both keep the constant-time compare, the 500 precedes the compare, and no em dashes remain. Then the full click path in a browser against both real handlers with the KV store stubbed: with the variable set and the real password the panel shows the waitlist, the build row and the staleness warning and the click downloads a complete 16,876,949 byte DMG with a koly trailer; with the variable set and the old published password the panel says "Wrong password."; with the variable unset the panel refuses, renders no waitlist and no download element at all, and the page source still contains no download URL for a visitor who has not authenticated. Could not verify: production and preview behaviour. Production already has ADMIN_PASS set so its behaviour is unchanged by this, and preview deployments sit behind Vercel deployment protection and cannot be reached anonymously. The unset case was therefore proved locally rather than on a deploy target.
finished: 2026-08-18T16:28:13Z

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and website/api/build.js both exist, and website/index.html contains both hero-waitlist and bento-cell. fix/fail-closed-admin-pass was cut fresh from origin/main.
- This file had committed conflict markers on main at lines 55, 60 and 85, from the merge the brief mentions. They are gone. Resolving them properly meant more than deleting three lines: the block above the separator was the bento entry's notes and the block below it was the waitlist-page entry's notes, and that same bad merge had deleted the whole body of the "Website rewrite - waitlist page" entry, leaving a bare heading. Its body was restored from the PR #13 merge commit, 93ef52b, rather than retyped, and its notes were put back underneath it. All five entries now have their eight fields and their own notes.
- Jobs 2 to 5 of the eleven day plan were deliberately not started. Job 2 is idle detection, snooze, hotkey, Do Not Disturb and hiding the overlay from screen sharing.
- Untouched on purpose, per the brief: the stale committed DMG and the red dmg-freshness check, and the absence of any GitHub release.

---

## Secret download serves the newest release asset
status: done
branch: fix/secret-download
pr: https://github.com/Jack-Attackz101/eyerest/pull/16
changed: website/api/build.js, website/index.html, .conductor-status.md
skipped: Nothing asked for. Two findings that need a human rather than code. (1) This repo has no GitHub releases at all, /releases/latest is a 404, so after this merges the panel keeps serving the committed DMG and now says "no release published yet, serving the committed DMG" under it. The fix is correct but changes nothing visible until a release exists with a .dmg asset attached. (2) ADMIN_PASS is not set in the Vercel project, so the fallback value in the source is what is live, and that value is committed to a public repo. The panel and the download are therefore open to anyone who reads the code. Nothing in this change made that worse and nothing in it can fix it; the env var needs setting in Vercel.
blocked_on: none
verified: Walked the flow on production in a real browser with downloads captured to disk BEFORE changing anything. Steps 1 to 4 all work; step 5 is the break and it is not a 404, it downloads a genuine mountable disk image (16,876,949 bytes, UDIF, Iris.app 1.0.0) that is simply the 29 July binary, 12 days behind the sources. Production also answered 401 with no password and 401 with a wrong password, and the page source for a logged-out visitor contained no download URL. After the change: the real handler was run against a stubbed GitHub API for every branch, 21 assertions passing, covering 401 for no, empty and wrong passwords with no download details in the 401 body, a release with a dmg returning the asset URL, tag label, size and published date and picking the dmg over the first asset, a release with no dmg falling back with that reason, no release falling back with that reason, a thrown fetch and a 403 both falling back with honest notes rather than erroring, the five minute cache making no second GitHub call, a failure not being cached, the 7 day threshold flagging 12 days and not flagging 1, and POST being refused. The release parsing was then run against a real GitHub /releases/latest payload rather than my own fixture, and a real release asset URL was fetched through its redirect to confirm it returns application/octet-stream with dmg magic bytes rather than an HTML error page, hdiutil reporting UDZO. The full click path was walked in a browser for both paths with downloads captured: the fallback panel reads "committed DMG, built Jul 29 2026, 16.1 MB, served ..." with the note and the 12 days warning and yields a complete file with a koly trailer, and with GitHub answering with a real release the panel reads "GitHub release v3.0.11, built Aug 9 2026, 7.1 MB" with no warning and the click downloads the asset as an attachment. The hero waitlist suite still passes, 27 assertions. Could not verify: the release path against this repo's own releases, because there are none, so another public repo's real release was used to exercise the same code; and the Vercel preview for this PR, because preview deployments are behind Vercel deployment protection and redirect to vercel.com/sso-api, so they cannot be reached anonymously. Production is public, which is why the before walk was possible at all.
finished: 2026-08-11T23:23:30Z

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and website/api/build.js both exist. fix/secret-download was cut fresh from origin/main, which by then had PRs #12, #13 and #15 merged.
- To make the download actually current, publish a release with Iris.dmg attached. The endpoint picks the first asset whose name ends in .dmg from /releases/latest, so the file name does not matter.
- The bento feature layout, PR #14, is still open and unmerged. It edits the features section of website/index.html; this branch edits the admin panel script and one CSS line, so they do not overlap.
- The dmg-freshness CI check will stay red until the committed DMG is rebuilt or removed. It compares the committed binary against the sources and knows nothing about releases, so once releases are the real download that check is measuring something the site no longer serves. Worth revisiting then.

---

## Hero waitlist box
status: done
branch: hero/waitlist-box
pr: https://github.com/Jack-Attackz101/eyerest/pull/15
changed: website/index.html, .conductor-status.md
skipped: Nothing asked for. Two notes on how the scope rule was kept: the form at the bottom was not touched at all, so the shared handler finds both forms by id rather than by adding an attribute to the lower one, and the hero form declares its own success behaviour with data-collapse-on-success. The one line added inside the existing mobile media query is hero-scoped (.hero-waitlist .wl-submit), which was unavoidable to make the button full width when the row stacks.
blocked_on: none
verified: Headless Chrome over the DevTools protocol against a local stand-in for the Vercel runtime, with the waitlist endpoint stubbed to exercise the new, duplicate and refused branches. 27 assertions, all passing. Structure: hero children in the required order (wordmark, slogan, supporting line, box, note, link); the supporting line is two clauses and says it is not out; placeholder you@example.com and button "Join the waitlist"; input and button on one row at equal height on desktop; the box reuses .waitlist-input and .wl-submit; the reassurance line is one short sentence at 11px in a muted colour; "See how it works" has no background and no shadow and carries an arrow, the submit button is the only solid element in the hero, and the link is centred under the box; nothing is autofocused; nothing overlaps and the whole hero fits the viewport. Behaviour: exactly one fetch to /api/waitlist in the file; the hero refuses an invalid address before posting; success collapses the box and leaves the confirmation in place without scrolling or opening a dialog; both forms work independently in both orders; both report a duplicate identically and a server refusal identically and stay usable after the error. Content: no price, no em dash, no countdown, no invented signup count, nothing sticky or fixed. At 390px the row stacks, the button goes full width, nothing overflows and it still submits. Scope was checked mechanically rather than by eye: nav through hero, header close through the script tag, features through the admin panel, and the CSS from the reveal block to the end of the stylesheet are all byte-identical to main apart from the one hero-scoped mobile rule. Screenshots at 1280px and 390px plus the post-signup state were reviewed. Not verified: the live deployment, since the Vercel preview is behind deployment protection and needs a logged-in browser.
finished: 2026-08-11T23:09:50Z

Notes for the next session:

- Branch check passed before any work: Iris/Engines/NudgeBudget.swift and website/api/build.js both exist. hero/waitlist-box was cut fresh from origin/main.
- Two waitlist forms now share one wire(form) function. If a third is ever added, give it a .waitlist-row, a .waitlist-msg, an input[type=email] and a submit button, then add its id to the list at the bottom of that function.
- The bento box feature layout is a separate branch and PR that is not merged: bento-box-sections-layout, https://github.com/Jack-Attackz101/eyerest/pull/14. It also edits website/index.html, in the features section, so it and this branch will both want main. They touch different parts of the file.
- CI: the placeholder check passes. The dmg-freshness check fails, as it does on main and every branch, because the shipped DMG is behind the source.

---

## Website rewrite - waitlist page
status: done
branch: redesign/waitlist-page
pr: https://github.com/Jack-Attackz101/eyerest/pull/13
changed: website/index.html, website/api/build.js (new), website/js/iris-demo.js (deleted), .conductor-status.md
skipped: Nothing from the six tasks. Two deliberate calls that are not what a literal reading would give: (1) the dark stats band still sits between features and how it works, where it already was. The required section order does not mention it and removing it would have changed the section rhythm the brief said to keep. All named sections are in the required order. (2) The build date in the admin panel is shown twice, labelled. The brief says to read it from the file's Last-Modified header, but Vercel stamps that header at deploy time, not build time, so a warning driven off it alone would never fire. The panel shows "served" from the header as asked and "built" from the commit that last touched website/Iris.dmg, and the staleness warning compares the build date against the newest commit under Iris/, the same comparison the DMG freshness CI check makes. website/images/cloudbkng.png is now unreferenced since it was only used by the deleted demo; left in place because deleting assets was not asked for.
blocked_on: none
verified: Served website/ locally through a stand-in for the Vercel runtime that runs the real api/build.js, then drove headless Chrome over the DevTools protocol. 34 assertions, all passing: page loads with no console errors (the only network error logged is the 401 the harness asks for on purpose with a wrong admin password); section order is hero, features, how it works, see it in action, waitlist, questions, footer; zero prices, zero Iris.dmg anywhere in the DOM, zero em dashes; hero has exactly one interactive element and it is centred under the slogan; nav CTA is "Join waitlist" pointing at #waitlist; the Supademo comment is where the demo was and the demo markup, script and importmap are gone; 26 feature items across 5 groups, each with a name and a sentence, both train photos kept, the unheaded paragraph gone; six details/summary questions with the default triangle removed, all starting closed; under real key events the summary takes focus, Enter opens, Space opens a second one while the first stays open, Enter closes; a real mouse click 30 px inside the right edge, past the question text, toggles the row; with prefers-reduced-motion emulated the accordion still opens and closes; the gated download renders no download element at all for a wrong password and shows the list, /Iris.dmg with a download attribute, "built Jul 29, 2026" and "This build is 11 days behind the source" for the right one; the waitlist form posts and confirms. Screenshots of hero, features, questions, waitlist and the unlocked admin panel were reviewed against the old design. Two harness bugs were found and fixed during this (a miscount and a smooth-scroll race), not page bugs. The harness lives in this workspace's gitignored .context/site-harness and is not committed. Not verified: the live deployment. The Vercel preview built fine but every request to it redirects to vercel.com/sso-api, so it is behind Vercel deployment protection and cannot be fetched without being logged in. The preview is where the served header and the GitHub API lookup first run against the real thing, so open it in a logged-in browser before merge: click the footer logo, enter the admin password, and check that the build row reads "built 29 Jul 2026 · served <the last deploy>" with the 11 days behind warning under it.
finished: 2026-08-10T22:22:47Z

Notes for the next session:

- Branch check passed before any work: Iris/FlowDetector.swift exists and SettingsComponents.swift contains SettingsTabBar. redesign/waitlist-page was cut fresh from origin/main.
- website/api/waitlist.js was not touched. The new endpoint reads the same ADMIN_PASS env var, so there is no second secret and no password in client-side JavaScript.
- Prompt 1 of the app build plan, the nudge budget, is a separate branch and PR: feat/nudge-budget, https://github.com/Jack-Attackz101/eyerest/pull/12. It is not merged, so it is not in this branch.
- CI on the PR: the placeholder check passes. The dmg-freshness check fails, as it does on main and every other branch, because the DMG shipped on the website predates the source by 11 days. That staleness is now visible in the admin panel, which was the point of task 6.
- There is a stash on this workspace from an earlier session: "wip on fix-dmg-bg-scene-only before feat/nudge-budget". It contains uncommitted edits to website/index.html among other files, so it will conflict with this rewrite if it is ever popped. Worth reviewing rather than applying blind.

---

## Website - bento box feature section
status: done
branch: bento-box-sections-layout
pr: https://github.com/Jack-Attackz101/eyerest/pull/14
changed: website/index.html, website/images/3.jpg (new), website/images/4.jpg (new), .conductor-status.md
skipped: Nothing asked for. Two notes on what was read into the request: the second train image was removed from the page rather than moved anywhere, and website/images/2.jpg is left in the repo unreferenced rather than deleted, since deleting the asset was not asked for. The two bento photographs are the two images supplied in the request, converted from PNG to JPEG at quality 82 to match the existing photography (90 KB and 98 KB, against 187 KB for images/1.jpg).
blocked_on: none
verified: Same headless Chrome harness as the previous pass, extended for the new structure. 39 assertions, all passing. New this round: the bento has exactly seven cells, two of which are the supplied photographs and both load; it is a bento rather than a plain grid, three rows with more than one cell width; every cell in a row matches the tallest in that row, so there are no half-empty boxes; the two lead items are stacked in one column beside the first train and the lead type is larger than the bento type; only images/1.jpg is left in the lead and images/2.jpg appears nowhere in the document; all 26 features are still listed with a name and a sentence each. Everything from the previous pass still passes: page loads with no console errors, section order, no prices, no public dmg links, no em dashes, six-question accordion under real key events, reduced motion, the gated download with its stale build warning (now reading 12 days, because the comparison is live), and the waitlist form. Mobile checked at 390px: one column, photographs at a 4:3 crop, no horizontal overflow. Full-section screenshots at desktop and mobile were reviewed against the previous design. Not verified: the live deployment, since the Vercel preview is behind deployment protection and needs a logged-in browser.
finished: 2026-08-11T22:55:43Z

Notes for the next session:

- This branch is cut from main after PR #13 merged, so it already contains the waitlist page rewrite. PR #12, the nudge budget, is also merged.
- Bento layout is column spans only, in source order: breaks, body, photo, focus, day, progress, photo. Rows are breaks and body, then photo with focus and day, then progress wide with the second photo. Adding a group means picking new spans that still sum to 6 per row.
- CI: the placeholder check passes. The dmg-freshness check fails, as it does on main and every branch, because the shipped DMG is now 12 days behind the source. That staleness is visible in the gated admin panel.
- The workspace still has a stash from an earlier session, "wip on fix-dmg-bg-scene-only before feat/nudge-budget". It touches website/index.html and will conflict with both website rewrites if popped. Review it rather than applying it blind.

---

## Prompt 1 - Nudge budget
status: done
branch: feat/nudge-budget
pr: https://github.com/Jack-Attackz101/eyerest/pull/12
changed: Iris/Engines/NudgeBudget.swift (new), Iris/AppDelegate.swift, Iris/Controllers/WindDownController.swift, Iris/Controllers/DeskResetController.swift, Iris/Views/Settings/DemoSettingsSection.swift, .conductor-status.md (new)
skipped: NudgeEngine, the periodic posture nudges, is not routed through the budget. It was not one of the five named callers, it does not call showTransientNudge, and it owns a 60 second pill with its own show/hide/reschedule loop, so routing it needs a denial callback and a rework of its visibility bookkeeping rather than a gate in front of a detector. It remains a nudge source that can stack with budgeted ones. The DEBUG demo rows also still call showTransientNudge directly, on purpose, since their job is to bypass the gate.
blocked_on: none
verified: Debug and Release both build clean with no warnings (xcodebuild, macOS). Behaviour checked by compiling the real Iris/Engines/NudgeBudget.swift unmodified against stub TimerEngine, FlowDetector and DebugConfig collaborators, fast-cycle on so 8 minutes is 8 seconds, and driving 20 assertions: every rule 3 and rule 4 block (idle, resting, suspended, overlay on screen, last minute before a break, in flow), three requests coalescing into one nudge, priority order in the combined text, the lowest priority request dropped as crowdedOut, the minimum gap refusing then clearing, a pending nudge dropped when flow starts mid-window without spending hourly budget, the fourth nudge allowed and the fifth refused as hourlyCap, and every possible combined line fitting the 44 character menu bar limit. All 20 pass. That last assertion caught a real defect, a 47 character pair, which is fixed. The harness lives in this workspace's gitignored .context/nudge-budget-harness and is not committed. Not verified in the running app: the budget gates on multi-hour timers, so watching it accept and refuse for real needs a long session with the DEBUG counters open.
finished: 2026-08-10T22:04:34Z

Notes for the next session:

- Branch check against origin/main passed before any work: Iris/FlowDetector.swift exists, SettingsComponents.swift contains SettingsTabBar, LicenseWindowController.swift contains $14 and not $35. The workspace was sitting on fix-dmg-bg-scene-only, 16 commits behind main, so feat/nudge-budget was cut fresh from origin/main. Uncommitted license, pricing, DMG and website edits that were in the working tree were stashed, not discarded (git stash list, message "wip on fix-dmg-bg-scene-only before feat/nudge-budget").
- Judgement calls that reviewers should look at are written up in the PR: the 90 second coalescing hold, the menu bar text limit moving from 22 to 44 characters, combined text ordering by priority, and "look away - 20 seconds" losing its em dash to satisfy the brand rule.
- CI on the PR: the placeholder check passes. The dmg-freshness check fails, but it fails on main and on every other branch too, because the DMG shipped on the website predates the source. Pre-existing and untouched by this work.
- Prompts 2 through 9 were deliberately not started.